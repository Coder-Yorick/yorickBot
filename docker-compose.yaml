version: '3'
services:
  redis:
    container_name: yredis
    image: redis:alpine
    restart: always
    ports:
     - "6379:6379"
  yorickbot:
    container_name: yorickbot
    build:
      context: .
      dockerfile: Dockerfile
    ports:
     - "8081:8081"
    depends_on:
     - redis
    environment:
    - REDIS_HOST=redis
    - REDIS_PORT=6379
    - REDIS_PWD=